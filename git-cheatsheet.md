# cheat sheet

git init -> initialize a directrory for git
git remote add origin <ssh> -> links remote repository with local one
git add . -> adds all files to staging state
git commit -m "" -> change staging state to commit with a fitting message
git push -u origin main -> upload all committed files to github, -u ... is only needed the first time
